import createScopeStore from './createScopeStore'
import Provider, {withStores} from './Provider'

export {createScopeStore, Provider, withStores}
export default {createScopeStore, Provider, withStores}
